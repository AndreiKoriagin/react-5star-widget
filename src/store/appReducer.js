export const initialState = {
  collectedRatings: [],
  currentRating: 0
};

export function appReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
